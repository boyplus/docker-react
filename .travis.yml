language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t boyplus/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true boyplus/docker-react npm run test
